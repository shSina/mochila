// sPost reffers to simple post
var itemTypes = ['sPost'];

// ad reffers to admin
// te reffers to teacher
// st reffers to student
var userTypes = ['te','st'];

var enums = {
	itemTypes:itemTypes,
	userTypes:userTypes
}
 
module.exports = enums;